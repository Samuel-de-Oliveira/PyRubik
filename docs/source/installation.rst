Install PyRubik
===============

Well, this is easy. You should install it via pip:

.. code-block:: bash

  pip install PyRubik

This will work for any system.

after install you would like to see how the library works,
then take a look in :doc:`usage` of the library.
